fastapi==0.115.0
uvicorn[standard]==0.30.6

# Persistencia simple (sin Supabase/Postgres — el frontend no lo requiere)
sqlalchemy==2.0.35
asyncpg==0.29.0
aiosqlite==0.20.0
supabase==2.7.4

# Auth: hashing de contraseñas + JWT propio (el frontend ya espera un
# Bearer token guardado en localStorage, no un flujo de Supabase)
# passlib[bcrypt] 1.7.4 no es compatible con bcrypt>=4.1 (bug conocido);
# se fija la versión de bcrypt explícitamente.
passlib==1.7.4
bcrypt==4.0.1
pyjwt==2.9.0

# Extracción de CV (PDF / DOCX)
pdfplumber==0.11.4
python-docx==1.1.2

# IA — proveedor configurable (ver app/shared/ai_client.py)
# httpx se fija <0.28 porque openai==1.51.0 aún usa un parámetro (`proxies`)
# que httpx 0.28 eliminó — versiones más nuevas de openai no lo necesitan,
# pero se fija httpx aquí para evitar el error en tiempo de ejecución.
httpx==0.27.2
openai==1.51.0

# Validación / requests
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0
python-multipart==0.0.9
